Examples
========

There is a subdirectory called :file:`examples` in the GitHub repo for
**ipywidgets**. In the :file:`samples` subdirectory are two directories,
:file:`notebooks` and :file:`development`.